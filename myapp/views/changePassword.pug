extends navbar

block content
    
    .container
        form(action="/validation/change-password" method="post")
            div.card.mx-auto.rounded.border-secondary(style="max-width:500px;")
                h5.card-header.text-capitalize.bg-danger.text-white
                    | Cambiar Contraseña
                .card-body
                    .form-group
                        label.h6(for='user') Contraseña Anterior 
                            span.text-danger  (*)
                        input#titulo.form-control(type='textarea' name="user" required)
                    .form-group
                        label.h6(for='passwd') Nueva Contraseña
                            span.text-danger  (*)
                        input#palabrasClave.form-control(type='password' name="passwd" required)
                    .form-group
                        label.h6(for='passwd') Repetir Contraseña
                            span.text-danger  (*)
                        input#palabrasClave.form-control(type='password' name="passwd" required)
                input.btn.btn-primary(type="submit")
